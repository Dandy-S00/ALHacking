## 2025-05-15 - Improving React Native Accessibility
**Learning:** Screen readers cannot always infer the purpose of custom interactive elements like `TouchableOpacity`. Explicitly providing `accessibilityLabel` and `accessibilityRole="button"` is crucial for making the app usable for visually impaired users.
**Action:** Always include accessibility attributes when using interactive components in React Native to ensure a consistent and inclusive user experience.
